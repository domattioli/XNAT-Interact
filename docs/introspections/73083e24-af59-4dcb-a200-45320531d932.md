# Introspection — session 73083e24-af59-4dcb-a200-45320531d932

repo: domattioli/XNAT-Interact
branch: claude/codebase-improvement-plan-sGjxz (PR #23)
date: 2026-06-05
skill: introspect@DomI v1.3.5
domi_pin: 7400694
session_type: research + planning (no app-code edits; docs/specs only)

## Pre-flight reconciliation
branch_policy_conflict: false   # harness + task both specify claude/codebase-improvement-plan-sGjxz
mcp_scope_gap: false            # task targets domattioli/{domi,xnat-interact}; both in MCP scope
label_scheme_mismatch: false    # no issue-label filtering this session

## Commits this session
- de74a2c docs: XNAT data-model reference (docs/XNAT_MODEL.md)
- fda3341 docs: Phase 6 spec-kit — XNAT alignment plan (specs/006-xnat-alignment/)
- 58f31aa docs: real-XNAT integration test runbook (docs/INTEGRATION_TEST_RUNBOOK.md)
# also: 2 research subagents (codebase pyxnat inventory + external pyxnat/XNAT docs),
# GitHub ecosystem survey (~70 repos). No src/ edits — analysis + planning only.

## Pains
- pain: wanted real push/pull round-trip vs real XNAT to learn how original src/ actually works; sandbox network policy blocked it.
  frequency: once
  severity: high          # blocked the operator's explicit goal entirely
  evidence: "docker pull tomcat:9-jre8-alpine -> production.cloudfront.docker.com 403 Forbidden"; "central.xnat.org/data/version -> 403". dockerd started OK, pyxnat 1.6.4 + xnat4tests installed; only image-pull + remote-server egress blocked.
  existing_skill_should_have_caught_it: none
  missing_skill_would_have_prevented_it: env-capability-preflight  # probe docker-pull + target-host egress BEFORE install+boot attempt; surface "live integration impossible here" in one step
  domi_issue: null        # single occurrence -> frequency gate (rule 11): corpus-log only, do not file
  saved_time_estimate_min: 4

- pain: `pip install xnat4tests` silently downgraded pydicom 3.0.2 -> 2.4.5 in the shared env (mutated repo's installed deps).
  frequency: once
  severity: low
  evidence: pip output "Uninstalling pydicom-3.0.2 ... Successfully installed ... pydicom-2.4.5". Also pulled xnat(xnatpy) 0.8.0.
  existing_skill_should_have_caught_it: none
  missing_skill_would_have_prevented_it: none — process gap (use a venv for throwaway test tooling, not the session interpreter)
  domi_issue: null
  saved_time_estimate_min: 1

- pain: introspect run from /home/user/DomI cwd gathered DomI's git log, not this session's actual work (XNAT-Interact); session was cross-repo (DomI = governance source, XNAT-Interact = work target).
  frequency: recurring-across-sessions   # multi-repo sessions are the DomI consumer-repo norm
  severity: medium
  evidence: run_introspection.sh emitted "Repo: unknown", session ref HEAD~10, commits 7400694..bc29b51 (all DomI), while real commits were de74a2c/fda3341/58f31aa in XNAT-Interact.
  existing_skill_should_have_caught_it: introspect
  missing_skill_would_have_prevented_it: none — introspect's OWN gap (script assumes cwd == session-work repo; should accept a --repo arg or detect the branch's repo)
  domi_issue: null        # DomI #9 VERIFIED CLOSED (operator 2026-05-23 "enough content") -> rule 8 no-comment-on-closed. Route = new request:skill if recurs; single-occurrence -> frequency gate (rule 11) -> corpus-only, re-evaluate on recurrence.
  saved_time_estimate_min: 2

## Wins (concrete, no theater)
- 16-call pyxnat surface fully inventoried (docs/XNAT_MODEL.md §3) -> proves a gateway ABC can wrap the whole XNAT dep. Drove Phase 6 plan.
- xnat4tests identified as the correct free/local/no-PHI real-XNAT test path; blocked only by env network policy, not approach. Runbook + handoff prompt left for an open-network relaunch.
- Caveman ultra held in chat; tiered subagent dispatch used for research (Explore + general-purpose). No policy violations.

## Routing decisions (verified against DomI open issues 2026-06-05)
- Scanned 13 open DomI `request: skill` issues. Match check vs this session's 3 pains:
  - env-block: closest #194 (cloud-harness CDN egress) is download-when-blocked, NOT preflight-whether-blocked -> would NOT have prevented this pain -> no honest vote (rule 3).
  - pydicom downgrade: process gap, no skill maps.
  - introspect cross-repo cwd: introspect's own forum = DomI #9, VERIFIED CLOSED -> rule 8 no-comment.
- 3 pains all single-occurrence-in-corpus (first corpus entry for this repo) -> frequency gate (rule 11): corpus-log only, ZERO new DomI issues filed, ZERO votes.
- DomI #9 closed (operator 2026-05-23) -> NO per-session retro comment (rules 5,7,8,9). Earlier intra-session offer to comment #9 was retracted on verifying closed state.
- Telemetry: corpus = canonical record (this file). No new tracking issue. Zero GitHub writes this session = correct disciplined outcome.
- Re-evaluate introspect-cross-repo-cwd + env-capability-preflight as new request:skill issues IF either recurs (>=2 in corpus).

## One-liner
introspection ~4 min, surfaced 3 pains (1 high env-block), all single-occurrence -> corpus-only; 13 open issues scanned, 0 matched-and-preventable -> 0 votes, 0 filings, 0 GitHub writes (protocol-correct restraint).
