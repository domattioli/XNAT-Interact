# streamlit: Streamlit web UI framework (Phase 2 app layer).
# No strict version pin — any recent stable release works; pin in environment.yml for reproducibility.
streamlit
certifi
charset-normalizer
contourpy
cycler
fonttools
idna
importlib_resources
ipykernel
kiwisolver
lxml
matplotlib
matplotlib-inline
numpy
opencv-python
packaging
pandas
pwinput
pydicom
pyparsing
python-dateutil
pytz
pyxnat
# nibabel: NOT used directly, but pyxnat's derivatives module imports it unguarded at
# Interface() construction (pyxnat/core/derivatives/bamos.py) -- without it main.py dies
# with ModuleNotFoundError even though update_and_test.py reports success (issue #10).
nibabel
requests
setuptools
six
tabulate
tzdata
urllib3
wheel
zipp
openpyxl
# --- 011 operational hardening. All three lazy-imported / opt-in; absent → graceful skip. ---
# SQLAlchemy: Postgres backend (US2). Lazy-imported in src/services/registry_backend.py only.
SQLAlchemy
# cryptography: AEAD crosswalk encryption (US1). Lazy-imported inside crosswalk_crypto.py fns.
cryptography
cffi  # cryptography's CFFI backend; pin explicitly — some envs ship it missing → Rust bindings panic.
# psycopg[binary]  # only when XNAT_REGISTRY_PG_DSN set — install manually for Postgres path.

# --- 010 pixel de-id (burned-in PHI) deps moved to requirements-pixeldeid.txt ---
# Those packages (presidio-image-redactor → python-gdcm) need a CMake toolchain and fail to
# build on stock macOS/Python 3.9, which would abort this whole install. They are lazy-imported
# (src/services/pixel_deid/* only) and not needed to run the app, the CLI, or the test suite.
# Install them separately ONLY for the pixel-deid lane:  pip install -r requirements-pixeldeid.txt
